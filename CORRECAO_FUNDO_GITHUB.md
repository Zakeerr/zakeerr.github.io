# Correção do fundo no GitHub Pages

O fundo do Hero deixou de ser um `<img>` absoluto e passou a ser aplicado diretamente na classe `.hero-section` por `background-image`.

Arquivos alterados:
- `components/hero.tsx`
- `app/globals.css`

Imagem usada:
- `public/fundo2.png`
